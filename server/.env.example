# Copy this file to the project root as .env (not committed).
# SPOTIFY_CLIENT_ID=your_client_id_from_developer_dashboard
# SPOTIFY_CLIENT_SECRET=your_client_secret_optional_for_pkce_public_clients
# OAuth redirect must match exactly:
# http://localhost:8888/callback
# PORT=8888
