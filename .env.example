# Copy to .env in the project root before running the token server or setup.
# Never commit real credentials.

SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=optional_for_pkce_public_clients
PORT=8888
